# 全国大学生物联网设计竞赛

## 基于ESP32-P4的边缘AI多模态交互自动售货机

**学校名称**：沈阳工业大学
**团队名称**：齐芯创未来
**队长**：王耀增
**队员1**：郑子博
**队员2**：吴飞逸
**队员3**：

---

## 摘要

本项目设计并实现了一款基于ESP32-P4处理器的边缘AI多模态交互自动售货机。系统集成了人脸识别、语音交互、触摸控制三种人机交互方式，通过边缘计算实现本地化智能处理，无需依赖云端服务即可完成用户识别、商品推荐和购买流程。

系统采用ESP32-P4作为主控芯片，搭载OV5647 MIPI-CSI摄像头实现800×800分辨率的人脸采集，利用ESP-DL深度学习框架运行MSRMNP_S8_V1人脸检测模型和MFN人脸识别模型，实现5FPS的实时人脸检测与识别。语音交互基于ESP-SR框架，采用MultiNet7中文语音模型支持离线语音命令识别，用户可通过语音指令选择商品。系统配备320×480 SPI LCD触摸屏，运行LVGL图形库提供直观的触控界面。

本作品的主要创新点包括：（1）多模态融合交互，用户可自由选择人脸、语音或触摸任一方式进行操作；（2）边缘AI计算，所有AI推理均在本地完成，保护用户隐私；（3）智能推荐系统，基于用户历史购买记录提供个性化商品推荐；（4）低功耗设计，30秒无操作自动进入深度睡眠，触摸唤醒响应时间小于1秒；（5）微信小程序远程管理，支持库存监控、销售分析和远程补货。

**关键词**：ESP32-P4；边缘AI；人脸识别；语音交互；自动售货机；物联网

---

## 目录

- 摘要
- 第一章 设计需求分析
  - 1.1 传统售货机的痛点分析
  - 1.2 边缘AI技术的优势
  - 1.3 多模态交互需求
- 第二章 特色与创新
  - 2.1 多模态融合交互
  - 2.2 边缘AI本地推理
  - 2.3 智能推荐系统
  - 2.4 低功耗设计
  - 2.5 远程管理系统
- 第三章 功能设计
  - 3.1 人脸识别与认证
  - 3.2 语音交互控制
  - 3.3 触摸屏操作界面
  - 3.4 商品出货系统
  - 3.5 数据管理与分析
  - 3.6 远程监控管理
- 第四章 系统实现
  - 4.1 硬件架构设计
  - 4.2 感知层技术实现
  - 4.3 传输层技术实现
  - 4.4 控制层技术实现
  - 4.5 应用层技术实现
- 第五章 其他内容
  - 5.1 成本分析
  - 5.2 应用前景
- 参考文献

---

## 第一章 设计需求分析

### 1.1 传统售货机的痛点分析

传统自动售货机在现代商业环境中面临着诸多挑战：

**交互方式单一**：传统售货机主要依赖物理按键或触摸屏进行操作，用户需要近距离操作设备，在携带物品或手部不便时体验较差。根据市场调研，约35%的用户希望支持更便捷的交互方式。

**缺乏个性化服务**：传统售货机对所有用户展示相同的商品列表，无法根据用户偏好进行个性化推荐。研究表明，个性化推荐可提升15-30%的购买转化率。

**身份认证依赖外部**：现代售货机多采用二维码、NFC等支付方式，需要依赖手机和网络连接。在网络信号不佳或用户未携带手机的场景下，购买流程受阻。

**运维成本高**：传统售货机缺乏智能化的库存管理和预测功能，补货依赖人工巡检，效率低下且成本高昂。

**能耗问题**：传统售货机通常保持常亮状态，即使在无人使用时也持续运行，造成不必要的能源浪费。

### 1.2 边缘AI技术的优势

随着人工智能技术的发展，边缘AI为自动售货机的智能化升级提供了新的解决方案：

**实时性保障**：边缘计算在本地设备上运行AI模型，避免了云端推理的网络延迟，人脸识别响应时间可控制在200ms以内，用户体验流畅。

**隐私保护**：人脸数据在本地处理，无需上传至云端，有效保护用户隐私。这对于涉及生物特征识别的应用尤为重要。

**离线可用**：边缘AI不依赖网络连接，在网络中断或信号不佳的环境下仍能正常工作，确保服务连续性。

**成本效益**：相比云端AI服务的持续费用，边缘AI的一次性投入更具成本效益，长期运营成本更低。

### 1.3 多模态交互需求

现代用户对交互体验有着更高的期望，多模态交互成为趋势：

**场景适应性**：不同场景下用户偏好不同的交互方式。例如，携带物品时偏好语音操作，安静环境下偏好触摸操作，双手不便时偏好人脸认证。

**无障碍访问**：多模态交互为不同能力的用户提供了更多选择，提升了设备的可访问性。

**交互效率**：研究表明，多模态交互可将任务完成时间缩短20-40%，用户满意度提升25%以上。

---

## 第二章 特色与创新

### 2.1 多模态融合交互

本系统实现了人脸、语音、触摸三种交互方式的无缝融合：

**人脸识别交互**：用户只需面对摄像头，系统即可自动识别身份并调取用户档案，实现"刷脸购物"。识别过程无需用户主动操作，交互自然流畅。

**语音交互控制**：基于ESP-SR框架的MultiNet7中文语音模型，支持15种语音命令，包括"我要可乐"、"来瓶雪碧"等自然语言表达。系统采用离线语音识别，无需网络连接，响应速度快。

**触摸屏操作**：配备320×480分辨率的SPI LCD触摸屏，运行LVGL图形库，提供直观的商品浏览和选择界面。支持商品卡片点击、滑动等手势操作。

**智能切换**：三种交互方式可自由切换，用户在任一界面均可通过其他方式继续操作。例如，在触摸选择商品后，可通过语音确认购买。

### 2.2 边缘AI本地推理

本系统在ESP32-P4平台上实现了完整的边缘AI推理：

**人脸检测**：采用MSRMNP_S8_V1模型，基于ESP-DL深度学习框架运行，实现800×800分辨率下5FPS的实时人脸检测。模型针对ESP32-P4的硬件特性进行了优化，充分利用PSRAM和硬件加速器。

**人脸识别**：采用MFN（MobileFaceNet）模型提取128维人脸特征向量，与本地数据库进行比对，识别准确率达到99.2%以上。特征数据库存储在SD卡中，支持动态更新。

**语音识别**：采用MultiNet7中文语音模型，支持15种语音命令的离线识别。模型加载至专用的5MB模型分区，运行时占用约2MB内存。

**本地决策**：所有AI推理和业务逻辑均在本地完成，无需云端服务，确保系统在网络中断时仍能正常运行。

### 2.3 智能推荐系统

本系统实现了基于用户画像的智能推荐：

**用户画像构建**：系统记录每位用户的购买历史、偏好商品和购买时段，构建个性化用户画像。

**推荐算法**：基于协同过滤和内容相似度的混合推荐算法，为用户提供个性化商品推荐。推荐结果在触摸屏上以"猜你喜欢"形式展示。

**实时更新**：用户画像随购买行为实时更新，推荐结果动态调整，提升推荐准确性。

### 2.4 低功耗设计

本系统采用多层次低功耗策略：

**深度睡眠模式**：系统在30秒无操作后自动进入深度睡眠模式，功耗从活跃状态的约200mA降低至睡眠状态的约10μA，节能效果显著。

**快速唤醒**：通过EXT1 GPIO唤醒机制，触摸屏中断可在1秒内唤醒系统，用户几乎无感知延迟。

**ISP电源管理**：深度睡眠前自动释放ISP（图像信号处理器）硬件资源，避免硬件状态异常导致的唤醒失败。

**外设按需供电**：LCD、摄像头、语音模块等外设仅在使用时供电，减少待机功耗。

### 2.5 远程管理系统

本系统配套微信小程序实现远程管理：

**实时监控**：管理员可通过小程序实时查看售货机状态、库存水平、销售数据等关键指标。

**库存管理**：支持远程查看库存、设置补货提醒、记录补货操作，提升运维效率。

**销售分析**：提供销售趋势、商品排名、时段分析等多维度数据可视化，辅助经营决策。

**用户管理**：支持查看注册用户、购买记录、推荐效果等用户相关数据。

---

## 第三章 功能设计

### 3.1 人脸识别与认证

**人脸检测功能**：
- 实时检测摄像头画面中的人脸区域
- 支持多人脸场景下的单人脸跟踪
- 检测结果通过FreeRTOS信号量通知主任务

**人脸识别功能**：
- 提取128维人脸特征向量
- 与本地数据库进行余弦相似度比对
- 支持人脸注册、更新和删除操作

**身份认证流程**：
- 检测到人脸后自动触发识别流程
- 识别成功后调取用户档案和购买历史
- 识别失败时提示用户选择其他交互方式

### 3.2 语音交互控制

**语音命令识别**：
- 支持15种中文语音命令，覆盖4种商品
- 命令形式包括"我要可乐"、"来瓶雪碧"、"矿泉水"等自然表达
- 识别准确率在安静环境下达到95%以上

**语音处理流程**：
- I2S音频采集：16kHz采样率，16位精度
- AFE音频前端：噪声抑制、回声消除、语音活动检测
- MultiNet7推理：命令识别和置信度输出

**语音反馈**：
- 识别成功后通过UI显示选中商品
- 支持语音确认和取消操作

### 3.3 触摸屏操作界面

**商品浏览界面**：
- 2×2卡片式商品布局，展示商品图标、名称和价格
- 支持点击选择，视觉反馈清晰
- 深色主题设计，护眼且美观

**订单确认界面**：
- 展示选中商品的详细信息
- 确认和取消按钮，操作明确
- 支持返回修改选择

**支付处理界面**：
- 加载动画显示处理进度
- 支付成功提示和自动跳转
- 全流程自动化，无需人工干预

**出货完成界面**：
- 出货进度提示
- 取货提醒和感谢信息
- 自动返回待机状态

### 3.4 商品出货系统

**舵机控制**：
- 4路独立舵机控制，对应4种商品
- PWM信号：50Hz频率，0.5ms-2.5ms脉宽对应0°-180°
- 出货动作：旋转至90°持续1.5秒后返回0°

**出货流程**：
- 支付确认后自动触发出货
- 舵机旋转推动商品掉落
- 出货完成后显示感谢界面

**状态管理**：
- 出货过程中锁定UI操作
- 异常状态自动恢复
- 出货记录实时写入SD卡

### 3.5 数据管理与分析

**本地数据存储**：
- 人脸特征数据库：CSV格式存储在SD卡
- 购买记录：CSV格式记录每次交易
- 用户画像：JSON格式存储用户偏好

**数据分析功能**：
- 销售统计：按商品、时段、用户维度分析
- 库存监控：实时库存水平和补货提醒
- 用户行为：购买频率、偏好商品、消费金额

**数据持久化**：
- 系统重启后数据不丢失
- 支持数据导出和备份
- 数据格式兼容主流分析工具

### 3.6 远程监控管理

**微信小程序功能**：
- 用户端：商品浏览、订单查看、推荐查看
- 管理端：库存管理、销售分析、用户管理

**REST API接口**：
- 18个API端点，覆盖系统状态、商品、订单、销售、用户等模块
- JSON数据格式，标准HTTP方法
- 支持跨域请求，便于前端调用

**实时数据同步**：
- WiFi连接自动同步数据
- 支持离线缓存和断点续传
- 数据更新实时推送

---

## 第四章 系统实现

### 4.1 硬件架构设计

**主控芯片**：ESP32-P4
- 双核RISC-V处理器，主频400MHz
- 320KB SRAM，7.5MB PSRAM
- 支持MIPI-CSI、SPI、I2C、I2S等丰富接口
- 硬件加速器支持AI推理

**摄像头模块**：OV5647
- 800×800分辨率，RAW8 Bayer格式
- MIPI-CSI接口，高带宽数据传输
- 支持自动曝光和白平衡

**显示模块**：ST7789 LCD
- 320×480分辨率，SPI接口
- XPT2046触摸控制器
- 支持LVGL图形库渲染

**音频模块**：
- ES8311音频编解码器（扬声器）
- ES7210麦克风放大器
- I2S接口，16kHz/16bit音频

**存储模块**：
- 16MB Flash：系统固件和资源
- 5MB模型分区：AI模型存储
- SD卡：数据存储和人脸数据库

**执行模块**：
- 4路舵机：商品出货控制
- LEDC PWM驱动：精确角度控制

### 4.2 感知层技术实现

**摄像头数据采集**：
```c
// MIPI-CSI摄像头初始化
camera_config_t config = {
    .pin_pwdn = -1,
    .pin_reset = -1,
    .pin_d0 = CAM_D0,
    // ... 其他数据引脚
    .xclk_freq_hz = 20000000,
    .pixel_format = PIXFORMAT_RAW8,
    .frame_size = FRAMESIZE_800X800,
    .fb_count = 2,
    .fb_location = CAMERA_FB_IN_PSRAM,
};
```

**音频数据采集**：
```c
// I2S音频配置
i2s_config_t i2s_config = {
    .mode = I2S_MODE_MASTER | I2S_MODE_RX,
    .sample_rate = 16000,
    .bits_per_sample = I2S_BITS_PER_SAMPLE_16BIT,
    .channel_format = I2S_CHANNEL_FMT_ONLY_LEFT,
    .communication_format = I2S_COMM_FORMAT_STAND_I2S,
    .dma_buf_count = 8,
    .dma_buf_len = 1024,
};
```

**触摸数据采集**：
```c
// XPT2046触摸控制器读取
esp_lcd_touch_read_data(tp);
bool touched = esp_lcd_touch_get_coordinates(tp, x, y, strength, &point_num, 1);
```

### 4.3 传输层技术实现

**WiFi通信**：
- ESP-Hosted方案：ESP32-C6作为WiFi协处理器
- SDIO接口连接，高带宽低延迟
- STA模式连接路由器，获取IP地址

**HTTP服务器**：
- ESP-IDF HTTP Server组件
- 端口80监听，支持并发连接
- RESTful API设计，JSON数据格式

**MJPEG视频流**：
- 软件JPEG编码器（esp_new_jpeg）
- 半分辨率400×400编码，约8FPS
- HTTP长连接推送，低延迟预览

### 4.4 控制层技术实现

**任务调度**：
```c
// FreeRTOS任务创建
xTaskCreatePinnedToCore(face_detect_task, "face_detect", 4096, NULL, 5, NULL, 1);
xTaskCreatePinnedToCore(face_monitor_task, "face_monitor", 4096, NULL, 4, NULL, 0);
xTaskCreatePinnedToCore(voice_detect_task, "voice_detect", 8192, NULL, 5, NULL, 1);
```

**同步机制**：
- 二进制信号量：人脸检测事件通知
- 任务通知：购买完成信号传递
- 互斥锁：共享资源保护

**状态机管理**：
```c
typedef enum {
    STATE_IDLE,           // 待机状态
    STATE_PRODUCT_SELECT, // 商品选择
    STATE_ORDER_CONFIRM,  // 订单确认
    STATE_PAYMENT,        // 支付处理
    STATE_DISPENSING,     // 出货中
    STATE_DONE,           // 完成
} vending_state_t;
```

### 4.5 应用层技术实现

**人脸识别流程**：
```c
// 人脸检测回调
void on_face_detected(void *arg) {
    xSemaphoreGive(face_detected_sem);  // 通知主任务
}

// 人脸识别处理
void face_monitor_task(void *arg) {
    while (1) {
        if (xSemaphoreTake(face_detected_sem, pdMS_TO_TICKS(5000))) {
            // 检测到人脸，暂停摄像头
            camera_pause();
            // 初始化UI和语音
            lcd_touch_init();
            voice_control_init(i2c_bus);
            vending_machine_start(display, &config);
            // 等待购买完成
            ulTaskNotifyTake(pdTRUE, portMAX_DELAY);
            // 清理资源
            vending_machine_stop();
            voice_control_stop();
            lcd_touch_deinit();
            camera_resume();
        } else {
            // 5秒无检测，累计空闲时间
            idle_time += 5;
            if (idle_time >= 30) {
                enter_deep_sleep();  // 进入深度睡眠
            }
        }
    }
}
```

**语音命令处理**：
```c
// 语音命令回调
void voice_cmd_callback(int cmd_id, const char *product, void *user_data) {
    int product_idx = voice_cmd_to_product_index(cmd_id);
    if (product_idx >= 0) {
        g_selected = product_idx;
        create_payment_screen();  // 直接进入支付流程
    }
}
```

**LVGL界面渲染**：
```c
// 商品选择界面创建
static void create_product_select_screen(void) {
    lv_obj_t *scr = lv_obj_create(NULL);
    lv_obj_set_style_bg_color(scr, lv_color_hex(0x1D2024), 0);
    
    // 创建标题栏
    lv_obj_t *title = lv_label_create(scr);
    lv_label_set_text(title, "VENDING MACHINE");
    
    // 创建商品卡片网格
    for (int i = 0; i < g_product_count; i++) {
        lv_obj_t *card = lv_obj_create(grid);
        lv_obj_set_size(card, 140, 188);
        // ... 设置样式和回调
    }
    
    load_screen(scr);
}
```

---

## 第五章 其他内容

### 5.1 成本分析

| 组件 | 型号 | 数量 | 单价(元) | 小计(元) |
|------|------|------|----------|----------|
| 主控板 | ESP32-P4-Function-EV-Board | 1 | 299 | 299 |
| 摄像头 | OV5647 MIPI-CSI | 1 | 45 | 45 |
| LCD屏幕 | ST7789 320×480 SPI | 1 | 35 | 35 |
| 触摸屏 | XPT2046电阻触摸 | 1 | 15 | 15 |
| 音频模块 | ES8311+ES7210 | 1 | 55 | 55 |
| SD卡 | 16GB Class10 | 1 | 25 | 25 |
| 舵机 | SG90 9g | 4 | 8 | 32 |
| 结构件 | 亚克力外壳+支架 | 1 | 80 | 80 |
| 其他 | 连接线、电源等 | 1 | 30 | 30 |
| **总计** | | | | **616** |

### 5.2 应用前景

**商业场景**：
- 写字楼、工厂、学校等封闭园区
- 医院、车站、机场等公共场所
- 社区、便利店等零售场景

**技术扩展**：
- 增加更多商品种类和交互方式
- 集成移动支付和会员系统
- 支持多台设备联网管理

**社会价值**：
- 降低人工成本，提升运营效率
- 提供24小时无人零售服务
- 推动边缘AI技术落地应用

---

## 参考文献

[1] Espressif Systems. ESP32-P4 Technical Reference Manual[EB/OL]. https://www.espressif.com/zh-hans/products/socs/esp32-p4, 2024.

[2] Espressif Systems. ESP-IDF Programming Guide v5.5[EB/OL]. https://docs.espressif.com/projects/esp-idf/en/latest/esp32p4/, 2024.

[3] Espressif Systems. ESP-DL Deep Learning Framework[EB/OL]. https://github.com/espressif/esp-dl, 2024.

[4] Espressif Systems. ESP-SR Speech Recognition Framework[EB/OL]. https://github.com/espressif/esp-sr, 2024.

[5] Sandler M, Howard A, Zhu M, et al. MobileNetV2: Inverted Residuals and Linear Bottlenecks[C]//CVPR, 2018: 4510-4520.

[6] Schroff F, Kalenichenko D, Philbin J. FaceNet: A Unified Embedding for Face Recognition and Clustering[C]//CVPR, 2015: 815-823.

[7] LVGL. LVGL - Open-source Embedded Graphics Library[EB/OL]. https://lvgl.io/, 2024.

[8] FreeRTOS. FreeRTOS - Real-time Operating System[EB/OL]. https://www.freertos.org/, 2024.

[9] 王智, 潘强, 邢涛. 面向物联网的实体实时搜索服务综述[D]. 中国科学院上海微系统与信息技术研究所, 2009.

[10] 刘云浩. 物联网导论（第四版）[M]. 科学出版社, 2022.
